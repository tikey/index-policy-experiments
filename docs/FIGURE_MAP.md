# Відповідність рисунків і журналів

- Рис. 1: Динаміка частоти порушень (за вікном) → `data/demo_event_log.jsonl` поля: `stats.p_hat_up`, `stats.delta`.
- Рис. 2: Частка ваги ризикової компоненти `alpha5` → `journal.theta.alpha5`.
- Рис. 3: Обчислювальна вартість на крок → `journal.cost.step_ms`.
